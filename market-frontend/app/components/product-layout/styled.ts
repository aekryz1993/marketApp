const productSectionClasses =
  "flex shrink grow max-w-full min-w-0 z-0";

const productContainerClasses = "min-h-full md:h-full max-w-full md:max-w-[calc(100%-48px)] md:min-h-0 bg-bg-light-sec dark:bg-bg-dark-sec rounded-lg md:self-center shadow-3xl md:flex md:flex-col overflow-y-auto md:overflow-hidden";

const productInfoSideClasses = "flex shrink-0 w-full rounded-r-lg border border-border-light-pry dark:border-border-dark-pry basis-[360px]"

const productInfoContainerClasses = "flex flex-col max-w-full overflow-hidden basis-full px-3 py-2 select-none"

export {
  productSectionClasses,
  productContainerClasses,
  productInfoSideClasses,
  productInfoContainerClasses,
};
