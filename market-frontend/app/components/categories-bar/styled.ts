const categoriesBarContainerClasses = "relative w-full";

const headerClasses = "text-2xl font-bold text-start";

const categoriesContainerClasses = "flex flex-col";

const categoryBoxClasses = "rounded-lg";

const categoryNameClasses = "font-medium text-lg";

export {
  categoriesBarContainerClasses,
  headerClasses,
  categoriesContainerClasses,
  categoryBoxClasses,
  categoryNameClasses,
};
