const formClasses = "relative w-full text-center";

const inputClasses = "w-full h-10 rounded-xl px-8";

const iconBoxClasses =
  "absolute h-10 px-2 bg-transparent flex items-center justify-center";
const iconClasses = "w-5 h-5 text-slate-400";

const loaderClasses = "w-8 h-8";

export {
  formClasses,
  iconBoxClasses,
  iconClasses,
  inputClasses,
  loaderClasses,
};
