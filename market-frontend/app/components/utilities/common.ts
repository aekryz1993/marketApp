import clsx from "clsx";

const centerTextClasses = "text-center p-1";

const iconBoxClasses = clsx(centerTextClasses, "cursor-pointer bg-transparent");

const iconClasses = "inline w-5 h-5 align-middle lg:w-6 lg:h-6";

const flexCenterHorizontally = "flex items-center";

const flexCenterVertically = "flex justify-center";

export {
  centerTextClasses,
  iconBoxClasses,
  iconClasses,
  flexCenterHorizontally,
  flexCenterVertically,
};
